# from python
# sudo apt-get install curl
# export DBNAME, USERNAME, PASSWORD, ENDPOINT, PORT 
# run init.sh // This will test the above credentials
# if init.sh return == 0
#   flask start server
# else:
#   error, could not start server, incorrect credentials
